# TaskFlow React Frontend

This is a small React (Vite) frontend scaffold for the existing TaskFlow backend.

Quick start:

```bash
cd frontend/react-app
npm install
npm run dev
```

The app proxies to the backend using the same-origin `/api` base path. The backend must be running (e.g. `node server.js`).
